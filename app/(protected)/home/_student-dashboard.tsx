import { HomeHero } from "@/components/student/home-hero"

/** Student landing: hero now, topic cards get added below in the next step. */
export function StudentDashboard() {
  return (
    <div className="min-h-full">
      <HomeHero />
    </div>
  )
}
